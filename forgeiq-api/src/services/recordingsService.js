// RecordingsService - placeholder for business logic
// Queries will move here as routes become more complex

module.exports = {
  // Future methods:
  // getUserRecordings(user_id, page, limit)
  // createRecording(user_id, data)
  // getRecordingById(id, user_id)
  // updateRecording(id, user_id, data)
  // deleteRecording(id, user_id)
};
