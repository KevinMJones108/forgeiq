// TranscriptsService - placeholder for business logic
// Queries will move here as routes become more complex

module.exports = {
  // Future methods:
  // createTranscript(recording_id, data)
  // getTranscriptById(id, user_id)
};
